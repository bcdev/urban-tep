Several Urban TEP WPS config file(s) to be deployed in Tomcat:
1. utep-wps.properties : ${catalina.home}/conf/utep

Make sure that "${catalina.home}/conf/utep" is included in "common.loader" inside ${catalina.home}/conf/catalina.properties file.